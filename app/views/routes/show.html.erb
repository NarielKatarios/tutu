<p>
  <strong>Наименование:</strong>
  <%= @route.title %>
</p>

<p>
  <strong>Дополнительные сведения:</strong>
  <%= @route.text %>
</p>

<p>
  <strong>Станции в маршруте:</strong>
    <% if @route.railway_stations.empty? %>
      Список пуст
    <% else %>
        <ul>
          <% @route.railway_stations.each_with_index do |st, i| %>
            <li><%= 'index: ' + (i+1).to_s + ', id: ' + st.id.to_s + ', title:' + st.title %></li>
          <% end %>
        </ul>
    <% end %>
<p>

<p>
  <strong>Порядок станций в маршруте:</strong>

<table border="1">
  <tr>
    <th>№ п/п</th>
    <th>ID</th>
    <th>Станции</th>
    <th colspan="3"></th>
  </tr>

  <% @route.railway_stations.each_with_index do |station, index| %>
  <tr>
      <td><%= (index+1).to_s %></td>
      <td><%= station.id.to_s %></td>
      <td><%= link_to station.title, railway_station_path(station) %></td>
      <td><%= station.station_number %></td>
      <td><%= link_to 'Переместить вверх', (index-1).to_s %></td>
      <td><%= link_to 'Переместить вниз' %></td>
  </tr>
  <% end %>
</table>
  </p>

    <strong>Поезда:</strong>
  <%- @route.trains.each do |train| %>
  <p><%= full_name(train) %></p>
  <%- end %>


<%= link_to 'Изменить', edit_route_path(@route) %>
<%= link_to 'Назад', routes_path %>